name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1

jobs:
  test:
    name: Test
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [macos-latest]
        rust: [stable]
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ matrix.rust }}
          components: rustfmt, clippy

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: ~/.cargo/registry
          key: ${{ runner.os }}-cargo-registry-${{ hashFiles('**/Cargo.lock') }}

      - name: Cache cargo index
        uses: actions/cache@v4
        with:
          path: ~/.cargo/git
          key: ${{ runner.os }}-cargo-git-${{ hashFiles('**/Cargo.lock') }}

      - name: Cache cargo build
        uses: actions/cache@v4
        with:
          path: target
          key: ${{ runner.os }}-cargo-build-target-${{ hashFiles('**/Cargo.lock') }}

      - name: Check formatting
        run: cargo fmt -- --check

      - name: Run clippy
        run: cargo clippy --all-targets --all-features -- -D warnings

      # simq-py is a pyo3 extension module (built with the "extension-module"
      # feature). It cannot be linked by a plain `cargo build`/`cargo test` —
      # those symbols are only resolved when the module is dlopen'd by a
      # Python interpreter. It's built and tested separately via maturin +
      # pytest (see simq-py/), so it's excluded from the Rust checks here.
      - name: Build
        run: cargo build --verbose --all-features --workspace --exclude simq-py

      - name: Run tests
        run: cargo test --verbose --all-features --workspace --exclude simq-py

      - name: Run doc tests
        run: cargo test --doc --verbose --workspace --exclude simq-py

  coverage:
    name: Code Coverage
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ${{ runner.os }}-cargo-coverage-${{ hashFiles('**/Cargo.lock') }}

      - name: Install cargo-tarpaulin
        run: cargo install cargo-tarpaulin --locked

      - name: Generate coverage report and enforce threshold
        run: |
          cargo tarpaulin --verbose --all-features --workspace --timeout 120 \
            --engine llvm --out Xml --output-dir coverage --fail-under 97.5 \
            --exclude simq-py \
            --exclude-files "simq-py/*" \
            --exclude-files "simq-sim/src/gpu.rs" "simq-macros/src/lib.rs" \
            --exclude-files "simq-backend/src/ibm_quantum.rs" "simq-gates/build.rs" \
            --exclude-files "simq-gates/src/compile_time_cache.rs" \
            --exclude-files "simq-state/src/simd.rs" \
            --exclude-files "simq-state/src/simd/diagonal.rs" \
            --exclude-files "simq-state/src/simd/kernels.rs"

  # benchmark:
  #   name: Benchmark
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4

  #     - name: Install Rust
  #       uses: dtolnay/rust-toolchain@stable

  #     - name: Run benchmarks
  #       run: cargo bench --no-fail-fast

  #     - name: Store benchmark result
  #       uses: benchmark-action/github-action-benchmark@v1
  #       with:
  #         tool: 'cargo'
  #         output-file-path: target/criterion/*/new/estimates.json
  #         github-token: ${{ secrets.GITHUB_TOKEN }}
  #         auto-push: true

  security:
    name: Security Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install cargo-audit
        run: cargo install cargo-audit

      - name: Run security audit
        # RUSTSEC-2025-0020 / RUSTSEC-2026-0177: pyo3 0.21.2, used only by
        # the optional simq-py Python bindings crate (built separately via
        # maturin, excluded from the Rust checks above). Upgrading pyo3 to
        # >=0.29 is a breaking change to simq-py's API tracked separately.
        run: cargo audit --ignore RUSTSEC-2025-0020 --ignore RUSTSEC-2026-0177

  # docs:
  #   name: Documentation
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4

  #     - name: Install Rust
  #       uses: dtolnay/rust-toolchain@stable

  #     - name: Build documentation
  #       run: cargo doc --no-deps --all-features
  #       env:
  #         RUSTDOCFLAGS: -D warnings
